<%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>
    <main class="container mx-auto bg-white shadow-lg rounded-lg p-8 mt-10 max-w-xl">
      <h2 class="text-3xl text-red-500 font-bold text-center mb-6">
        Here are your favourites:
      </h2>
      <ul class="space-y-4">
        <% fil.forEach(home => { %>
          <li class="bg-gray-100 p-4 rounded-lg shadow-md hover:shadow-lg transition duration-300">
            <h3>House Name : </h3>
            <span class=" <% %>text-lg text-gray-800"><%= home.houseName %></span>
            <h3>Price per night : </h3>
            <span class="text-lg text-gray-800"><%= home.price %></span>
            <h3>Rating : </h3>
            <span class="text-lg text-gray-800"><%= home.rating %></span>
            <h3>Image : </h3>
            <img 
            src="<%= home.urll %>" 
            alt="House Image"
            class="w-full h-48 object-cover rounded-md mb-4"
            <div>
              <button type="submit" class="bg-amber-50 p-2">Book</button>
            </div>
        <div>
          <form action="/store/Favourite/delete/<%= home.id %>" method="POST">
            <button class="bg-red-300 p-2" type="submit">Remove from favourite</button>
          </form>
        </div>
          </li>
        <% }) %>
      </ul>
    </main>
  </body>
</html>